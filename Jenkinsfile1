pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh whoami
                touch file.tx

            }
        }

        stage('Test') {
            steps {
                sh df -h
                sh touch team6
            }
        }

        stage(backup){
            steps{
                sh pwd
                sh du -h
                echo 'webhook wahala'
            }
        }
    }
}